function forLoop(array) {
   for ( let i = 0; i < 25; i++) {
     if (i === 1) {
       push.array(`I am 1 strange loop.`)
     } else {
       push.array(`I am ${i} strange loops.`)
     }
  }
  return array;
}
